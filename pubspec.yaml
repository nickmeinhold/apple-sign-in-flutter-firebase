name: apple_sign_in_flutter_firebase
description: Documenting using the sign_in_with_apple and firebase_auth plugins.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  sign_in_with_apple: 
  firebase_auth:

  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
